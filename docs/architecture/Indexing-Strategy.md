# Indexing Strategy

IDX-001 Booking(court_id,start_time,end_time)
IDX-002 Customer(phone)
IDX-003 Lead(phone)
IDX-004 PricingRule(sport_id,date)
IDX-005 Gallery(sport_id,event_id)

Use composite indexes for booking searches.
