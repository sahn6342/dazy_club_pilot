# Backend Quick Reference

FastAPI (Python 3.12) + Pydantic v2 + SQLAlchemy 2.0 (sync) + Alembic + SQLite.

See `docs/CONTEXT.md` for full API endpoint list, DB schema, and business rules.
See `docs/architecture/Backend-Architecture.md` for layer structure.
See `apps/api/` for source code.
