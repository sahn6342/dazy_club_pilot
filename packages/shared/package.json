{
  "name": "@dazy/shared",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "echo \"No lint configured for shared yet.\"",
    "test": "echo \"No tests configured for shared yet.\"",
    "format": "echo \"No formatter configured for shared yet.\""
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
